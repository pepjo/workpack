% !TEX root = ../../DOCUMENT.tex

\begin{longtable}{|p{.15\textwidth}|p{.40\textwidth}|p{.19\textwidth}|p{.13\textwidth}|p{.13\textwidth}|}
  \hline
  ID & Activity & Predecessors & Relation & Lag \\\hline
  {{#each data}}
    % -- Group:
    {{{group.code}}} & \multicolumn{4}{c|}{ {{{group.name}}} } \\\hline
    % -- Tasks:
    {{#each workpacks}}
      {{{wsb_id}}} & 
      {{{activity}}} & 
      \begin{tabular}{@{}l@{}}{{#each predecessors}}{{{wsb_id}}} {{#unless @last}} \\[-10pt] {{/unless}} {{/each}}\end{tabular} & 
      \begin{tabular}{@{}l@{}}{{#each predecessors}}{{{_pivot_relation}}} {{#unless @last}} \\[-10pt] {{/unless}} {{/each}}\end{tabular} & 
      \begin{tabular}{@{}l@{}}{{#each predecessors}}{{{_pivot_lag}}} {{#unless @last}} \\[-10pt] {{/unless}} {{/each}}\end{tabular} \\\hline
    {{/each}}
  {{/each}}
  \caption{List of activities and dependencies or logical relationship between activities}\label{table3_1}
\end{longtable}
