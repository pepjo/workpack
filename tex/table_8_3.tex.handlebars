% !TEX root = ../../DOCUMENT.tex

\begin{longtable}{|p{.125\textwidth}|p{.125\textwidth}|p{.125\textwidth}|p{.125\textwidth}|p{.125\textwidth}|p{.125\textwidth}|p{.125\textwidth}|p{.125\textwidth}|}
  \hline
  WBS-ID & Resource & Direct costs & Method & C.L. & Reserve & Indirect Costs & Estimate \\\hline
  {{#each data}}
    % -- Group:
    {{{group.code}}} & \multicolumn{7}{p{.87\textwidth}|}{ {{{group.name}}} } \\\hline
    {{#each workpacks}}
    {{#if isTask}}
    % -- Task:
      {{{wsb_id}}} & 
      {{#each resources}}{{r_id}}; {{/each}} & 
      {{{c_cost_estimate}}}€ & 
      {{{c_type}}} & 
      {{{a_e_confidence_level}}} & 
      {{{a_e_reserve}}}€ & 
      {{{a_e_indirect_costs}}}€ & 
      {{{a_e_estimate}}}€ \\\hline
    {{else}}
    % -- WP:
      {{{wsb_id}}} & \multicolumn{7}{p{.87\textwidth}|}{ {{{activity}}} } \\\hline
    {{/if}}
    {{/each}}
  {{/each}}
  \caption{List of activity cost estimation}\label{table8_3}
\end{longtable}
