% !TEX root = ../../DOCUMENT.tex

\begingroup
\setlength{\LTleft}{-20cm plus -1fill}
\setlength{\LTright}{\LTleft}

\begin{longtable}{|p{.2\textwidth}|p{.2\textwidth}|p{.2\textwidth}|p{.2\textwidth}|p{.2\textwidth}|}
  \hline
  \multicolumn{5}{|c|}{\textbf{Parametric Estimates}} \\\hline
  WBS-ID & Variable Cost & Cost per unit & Number of units & Cost estimate \\\hline
  {{#each data}}
    % -- Group:
    {{{group.code}}} & \multicolumn{4}{p{.8\textwidth}|}{ {{{group.name}}} } \\\hline
    {{#each workpacks}}
    {{#if isTask}}
      {{#if isCp}}
      {{#each paramCosts}}
    % -- Task:
      {{{../wsb_id}}} & 
      {{{variable_cost}}} & 
      {{{cost_per_unit}}} \EUR{} & 
      {{{number_of_units}}} & 
      {{{estimate}}} \EUR{} \\\hline
      {{/each}}
      {{/if}}
    % {{else}}
    % % -- WP:
    %   {{{wsb_id}}} & \multicolumn{4}{c|}{} \\\hline
    {{/if}}
    {{/each}}
  {{/each}}
  \caption{List of parametric estimates}\label{table8_2_1}
\end{longtable}

\begin{longtable}{|p{.166\textwidth}|p{.166\textwidth}|p{.166\textwidth}|p{.166\textwidth}|p{.166\textwidth}|p{.166\textwidth}|}
  \hline
  \multicolumn{6}{|c|}{\textbf{Analogous Estimates}} \\\hline
  WBS-ID & Previous Activity & Previous Cost & Current Activity & Multiplier & Cost Estimate \\\hline
  {{#each data}}
    % -- Group:
    {{{group.code}}} & \multicolumn{5}{p{.75\textwidth}|}{ {{{group.name}}} } \\\hline
    {{#each workpacks}}
    {{#if isTask}}
      {{#if isCa}}
    % -- Task:
      {{{wsb_id}}} & 
      {{{c_a_previous_activity}}} {{{c_a_previous_activity_txt}}} & 
      {{{c_a_previous_cost}}} \EUR{} & 
      {{{c_a_current_activity}}} {{{c_a_current_activity_txt}}} & 
      {{{c_a_multiplier}}} & 
      {{{c_cost_estimate}}} \EUR{} \\\hline
      {{/if}}
    % {{else}}
    % % -- WP:
    %   {{{wsb_id}}} & \multicolumn{5}{c|}{} \\\hline
    {{/if}}
    {{/each}}
  {{/each}}
  \caption{List of analogous estimates}\label{table8_2_2}
\end{longtable}

\begin{longtable}{|p{.166\textwidth}|p{.166\textwidth}|p{.166\textwidth}|p{.156\textwidth}|p{.186\textwidth}|p{.156\textwidth}|}
  \hline
  \multicolumn{6}{|c|}{\textbf{Three Point Estimates}} \\\hline
  WBS-ID & Optimistic Cost & Most Likely Cost & Pessimistic Cost & Weighting Equation & Expected Cost Estimate \\\hline
  {{#each data}}
    % -- Group:
    {{{group.code}}} & \multicolumn{5}{p{.75\textwidth}|}{ {{{group.name}}} } \\\hline
    {{#each workpacks}}
    {{#if isTask}}
      {{#if isC3}}
    % -- Task:
      {{{wsb_id}}} & 
      {{{c_3_optimistic_cost}}} \EUR{} & 
      {{{c_3_mostlikely_cost}}} \EUR{} & 
      {{{c_3_pessimistic_cost}}} \EUR{} & 
      {{{c_3_wheighting_equation}}} & 
      {{{c_cost_estimate}}} \EUR{} \\\hline
      {{/if}}
    % {{else}}
    % % -- WP:
    %   {{{wsb_id}}} & \multicolumn{5}{c|}{} \\\hline
    {{/if}}
    {{/each}}
  {{/each}}
  \caption{List of three point estimates}\label{table8_2_3}
\end{longtable}

\endgroup
