% !TEX root = ../../DOCUMENT.tex

\begin{longtable}{|p{.15\textwidth}|p{.25\textwidth}|p{.12\textwidth}|p{.38\textwidth}|}
  \hline
  WBS-ID & Resource Id & Amount & Assumptions \\\hline
  {{#each data}}
    % -- Group:
    {{{group.code}}} & \multicolumn{3}{p{.75\textwidth}|}{ {{{group.name}}} } \\\hline
    {{#each workpacks}}
    {{#if isTask}}
    % -- Task:
      {{{wsb_id}}} & \
      {{#each resources}}{{{r_id}}}; {{/each}} & \
      {{#each resources}}{{{_pivot_amount}}}; {{/each}} & \
      {{{assumptions}}} \\\hline
    {{else}}
    % -- WP:
      {{{wsb_id}}} & \multicolumn{3}{p{.75\textwidth}|}{ {{{activity}}} } \\\hline
    {{/if}}
    {{/each}}
  {{/each}}
  \caption{List of resource requirement}\label{table4_2}
\end{longtable}

