% !TEX root = ../../DOCUMENT.tex

\begingroup
\setlength{\LTleft}{-20cm plus -1fill}
\setlength{\LTright}{\LTleft}

\begin{longtable}{|p{.166\textwidth}|p{.166\textwidth}|p{.166\textwidth}|p{.166\textwidth}|p{.166\textwidth}|p{.166\textwidth}|}
  \hline
  \multicolumn{6}{|c|}{Parametric Estimates} \\\hline
  WBS-ID & Effort Hours & Resource quantity & \% Available & Performance Factor & Duration Estimate (h) \\\hline
  {{#each data}}
    % -- Group:
    {{{group.code}}} & \multicolumn{5}{p{.75\textwidth}|}{ {{{group.name}}} } \\\hline
    {{#each workpacks}}
    {{#if isTask}}
      {{#if isTp}}
    % -- Task:
      {{{wsb_id}}} & 
      {{{t_p_effort_hours}}} & 
      {{{t_p_resource_quantity}}} & 
      {{{t_p_percentage_avaiable}}} & 
      {{{t_p_performance_factor}}} & 
      {{{t_duration_estimate}}} \\\hline
      {{/if}}
    % {{else}}
    % % -- WP:
    %   {{{wsb_id}}} & \multicolumn{5}{c|}{} \\\hline
    {{/if}}
    {{/each}}
  {{/each}}
  \caption{List of parametric estimates}\label{table5_1}
\end{longtable}

\begin{longtable}{|p{.166\textwidth}|p{.166\textwidth}|p{.166\textwidth}|p{.166\textwidth}|p{.166\textwidth}|p{.166\textwidth}|}
  \hline
  \multicolumn{6}{|c|}{Analogous Estimates} \\\hline
  WBS-ID & Previous Activity & Previous Duration & Current Activity & Multiplier & Duration Estimate \\\hline
  {{#each data}}
    % -- Group:
    {{{group.code}}} & \multicolumn{5}{p{.75\textwidth}|}{ {{{group.name}}} } \\\hline
    {{#each workpacks}}
    {{#if isTask}}
      {{#if isTa}}
    % -- Task:
      {{{wsb_id}}} & 
      {{{t_a_previous_activity}}} {{{t_a_previous_activity_txt}}} & 
      {{{t_a_previous_duration}}} {{{t_a_previous_duration_txt}}} & 
      {{{t_a_current_activity}}} {{{t_a_current_activity_txt}}} & 
      {{{t_a_multiplier}}} & 
      {{{t_duration_estimate}}} \\\hline
      {{/if}}
    % {{else}}
    % % -- WP:
    %   {{{wsb_id}}} & \multicolumn{5}{c|}{} \\\hline
    {{/if}}
    {{/each}}
  {{/each}}
  \caption{List of analogous estimates}\label{table5_2}
\end{longtable}

\begin{longtable}{|p{.166\textwidth}|p{.166\textwidth}|p{.166\textwidth}|p{.156\textwidth}|p{.186\textwidth}|p{.156\textwidth}|}
  \hline
  \multicolumn{6}{|c|}{Three Point Estimates} \\\hline
  WBS-ID & Optimistic Duration & Most Likely Duration & Pessimistic Duration & Weighting Equation & Expected Duration Estimate \\\hline
  {{#each data}}
    % -- Group:
    {{{group.code}}} & \multicolumn{5}{p{.75\textwidth}|}{ {{{group.name}}} } \\\hline
    {{#each workpacks}}
    {{#if isTask}}
      {{#if isT3}}
    % -- Task:
      {{{wsb_id}}} & 
      {{{t_3_optimistic_duration}}} & 
      {{{t_3_mostlikely_duration}}} & 
      {{{t_3_pessimistic_duration}}} & 
      {{{t_3_wheighting_equation}}} & 
      {{{t_duration_estimate}}} \\\hline
      {{/if}}
    % {{else}}
    % % -- WP:
    %   {{{wsb_id}}} & \multicolumn{5}{c|}{} \\\hline
    {{/if}}
    {{/each}}
  {{/each}}
  \caption{List of three point estimates}\label{table5_3}
\end{longtable}

\endgroup
