<table cellspacing="0" class="dataList">
  <tr>
    <th>Id</th>
    <th>Nom</th>
  </tr>
  {{#each group}}
    <tr>
      <td>{{this.id}}</td>
      <td>{{this.name}}</td>
    </tr>
  {{/each}}
</table>